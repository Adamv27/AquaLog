import { RequestHandler } from "express";
import NitrogenLogModel from "../models/nitrogenLog";

export const getNitrogenLogs: RequestHandler = async (req, res, next) => {
  try {

  } catch (error) {
    next(error);
  }
}